const checkCollection = require("./check-collection");
const contextDocuments = require("./data-cleanup");
const checkCollection = require("");

module.exports = {
  checkCollection,
  contextDocuments,
};
